# TelStore Telegram Bot — Python dependencies
#
# This bot uses ONLY Python's standard library (urllib) — it works out of the
# box with a plain Python 3.8+ install, no packages required.
#
# The packages below are OPTIONAL conveniences used only during development /
# testing (not needed to run the bot):
#   requests  - cleaner HTTP for quick API spikes
#   pytest    - run the test suite
#
# To install everything (optional):
#   pip install -r requirements.txt

requests>=2.28
pytest>=7.0
python-dotenv>=1.0
